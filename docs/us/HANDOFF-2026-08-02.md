# 인수인계 — US Alpaca Paper (2026-08-02)

> **새 채팅에서:** Cursor에서 **`us-swing-auto-trading` 폴더**를 워크스페이스로 연 뒤  
> `@docs/HANDOFF-2026-08-02.md` 를 첨부하고 **「이 HANDOFF 읽고 이어서」** 라고 하면 된다.

**최종 갱신:** 2026-08-02 저녁 KST  
**프로젝트 경로:** `C:\Users\KangilLee\Documents\projects\us-swing-auto-trading`  
**국내 레포 (참고만, 분리 운영):** `C:\Users\KangilLee\Documents\projects\swing-auto-trading`

---

## 0. 30초 요약

| | |
|---|---|
| **목적** | 미국 주식 스윙 자동매매 — **Alpaca Paper → (나중) Live** |
| **브로커** | Alpaca (KIS/키움과 **무관**) |
| **현재 단계** | **Phase 1 완료** — Paper API 키 설정 + probe 성공 |
| **다음 단계** | Phase 2: US 유니버스·백테스트 → Phase 3: `daily_trade` |
| **Git** | 아직 `git init` 안 함 (로컬만) |

---

## 1. 왜 별도 프로젝트인가

- 국내 `swing-auto-trading`: KIS 모의 + 키움 실전, KRW, KST 슬롯
- 본 프로젝트: Alpaca USD, US ET 슬롯, **시크릿·로그·워크플로 완전 분리**
- 국내에서 이식할 것: **패턴만** (guards, cooldown, ledger, probe-first) — 코드 복사 X

설계 전체: [`docs/DESIGN.md`](DESIGN.md)

---

## 2. Alpaca 계정 상태 (사용자 확인됨)

| 항목 | 값 |
|------|-----|
| 모드 | **Paper Trading** |
| Paper 계정 표시 | `Paper - PA3W8IZ3GAJE` (대시보드) |
| MFA | 설정 완료 (Secure Your Account 통과) |
| API Keys | Home → **Your API Keys** 에서 발급 (사이드바 「API」는 **문서**로 이동 — 키 발급 아님) |
| Live | **미개설** — 「Open Live Account」는 아직 누르지 않음 |

---

## 3. Phase 1 probe 결과 (2026-08-02, read-only)

명령:

```powershell
cd C:\Users\KangilLee\Documents\projects\us-swing-auto-trading
uv run python -m automation.probe
```

결과 요약:

| 필드 | 값 | 비고 |
|------|-----|------|
| status | ACTIVE | |
| currency | USD | |
| **cash** | **100,000** | ledger/ROI 기준으로 **이 값 사용** |
| buying_power | 400,000 | Paper 4x margin — **주문 한도로 단독 사용 금지** |
| portfolio_value / equity | 100,000 | |
| positions | 0 | |
| clock | 장 마감 (`is_open: false`) | 다음 개장 2026-08-03 09:30 ET |

**`.env` 권장값 (이미 example과 동일):**

```env
US_INITIAL_CAPITAL=100000
ALPACA_PAPER=true
ALPACA_LIVE_ENABLED=false
```

⚠️ `buying_power`(40만)을 초기 자본으로 쓰지 말 것 — 국내 KIS `total_eval`/키움 레버리지 필드와 같은 함정.

**아직 안 한 것 (선택):**

```powershell
$env:ALPACA_PROBE_ORDER="true"
uv run python -m automation.probe
```

→ $1 지정가 1주 매수 제출(체결 안 될 가능성 큼). 실제 체결·주문 API 검증용.

---

## 4. 프로젝트 파일 맵

```
us-swing-auto-trading/
├── .env                    ← 로컬만 (gitignore). API 키 있음 — 절대 커밋/채팅 금지
├── .env.example
├── README.md
├── AGENTS.md
├── pyproject.toml          ← alpaca-py, python-dotenv
├── docs/
│   ├── DESIGN.md           ← Paper→Live 로드맵
│   └── HANDOFF-2026-08-02.md  ← 이 파일
├── core/
│   ├── config.py           ← env 로딩
│   ├── guards.py           ← Live fail-closed
│   └── broker/alpaca_client.py
├── automation/
│   └── probe.py            ← Phase 1
└── tests/test_guards.py    ← 4 passed
```

---

## 5. Paper / Live 가드 (실전 대비)

| env | Paper (현재) | Live (나중) |
|-----|--------------|-------------|
| `ALPACA_PAPER` | `true` | `false` |
| `ALPACA_LIVE_ENABLED` | `false` | **`true` 필수** |
| API keys | Paper dashboard keys | Live keys (**별도**) |

둘 중 하나만 빠지면 `GuardError` — 국내 `KIWOOM_ENABLED` 패턴과 동일.

---

## 6. 로드맵 — 어디까지 / 다음에 뭘

| Phase | 상태 | 내용 |
|-------|------|------|
| **0** 골격 | ✅ | repo scaffold, guards, probe |
| **1** Paper 프로브 | ✅ | API 키, probe read-only 성공 |
| **2** 전략·백테스트 | 🔲 | US 유니버스, historical bars, 국내 momentum/ADX 이식 여부 결정 |
| **3** Paper 자동매매 | 🔲 | `daily_trade.py`, `logs/trades_*.jsonl`, ET 슬롯, GHA |
| **4** Live | 🔲 | KYC, USD 입금, Live keys, 수동 확인 후 첫 실행 |

### Phase 2 착수 시 추천 순서

1. `US_INITIAL_CAPITAL=100000` 이 `.env`에 반영됐는지 확인
2. 유니버스 후보: S&P500 / liquid large-cap (티커 기준)
3. `StockHistoricalDataClient` 또는 Polygon 등 데이터 소스 결정
4. 국내 `strategies/momentum_absolute.kis.yaml` + ADX 로직을 US용으로 이식할지 별도 전략할지 결정
5. 국내 PIT 유니버스 교훈 참고: `swing-auto-trading/docs/superpowers/specs/2026-07-28-pit-universe-strategy-revalidation-design.md`

### Phase 3 설계 초안 (DESIGN.md)

- ET 슬롯: 10:05 ET (장+35분), 15:15 ET (마감-45분) — 국내 2회/일 패턴
- `CASH_RESERVE=0.05`, `EXIT_COOLDOWN_TRADING_DAYS=2` — env에 이미 정의

---

## 7. 자주 쓰는 명령

```powershell
cd C:\Users\KangilLee\Documents\projects\us-swing-auto-trading
uv sync
uv run python -m automation.probe
uv run pytest -q
```

`.env` 편집: Cursor에서 `.env` 열기 또는 `notepad .env`

---

## 8. 국내 레포 참고 링크 (읽기 전용)

| 주제 | 경로 |
|------|------|
| 국내 운영 HANDOFF | `swing-auto-trading/docs/superpowers/HANDOFF-2026-08-02-full.md` |
| 해외 2단계 메모 (KIS — **본 프로젝트는 Alpaca로 대체**) | `swing-auto-trading/docs/superpowers/specs/2026-07-26-domestic-only-test-status.md` |
| 가드·cooldown 패턴 | `swing-auto-trading/automation/guards.py`, `daily_trade.py` |

---

## 9. 보안 체크리스트

- [x] API 키는 `.env`에만 (gitignore)
- [ ] `git init` 후에도 `.env` 커밋하지 않기
- [ ] GitHub Secrets 쓸 때 `ALPACA_*` — 국내 `KIS_*`/`KIWOOM_*` 와 이름 분리
- [ ] Live 전환 전 `ALPACA_LIVE_ENABLED` + 수동 확인

---

## 10. 새 채팅 시작 템플릿

```
워크스페이스: C:\Users\KangilLee\Documents\projects\us-swing-auto-trading
@docs/HANDOFF-2026-08-02.md

Phase 2(US 유니버스 + 백테스트)부터 이어서 진행해줘.
Alpaca Paper, Live는 나중에 같은 스택으로 갈 예정.
```

원하는 작업 예:

- 「Phase 2 백테스트 스캐폴드 만들어줘」
- 「국내 momentum 전략을 US 티커로 이식 설계해줘」
- 「git init 하고 첫 커밋 메시지 제안해줘」

---

## 11. 미해결 / 확인 필요

| # | 항목 | 비고 |
|---|------|------|
| 1 | 주문 탐침 (`ALPACA_PROBE_ORDER`) | 선택 — 아직 미실행 |
| 2 | git 저장소 | 미초기화 — 원하면 Phase 2 전에 init |
| 3 | Market Data 구독 | Alpaca free tier로 Phase 2 가능 여부 확인 필요 |
| 4 | PDT 규칙 | 스윙이면 영향 적으나 Live 전 `$25k` 미만 day-trade 제한 인지 |
